"""gtfs evidence adapter -- CONTRACT STUB (see the self-demo spec; Milestone TBD).

Turns gtfs source records into a standard CorridorEvidence (reference_points + reference_segments)
so both engines match it identically. Implement to complete the gtfs self-demo case.
"""


def to_evidence(*args, **kwargs):
    raise NotImplementedError("gtfs adapter: contract stub -- to be implemented per the self-demo spec")
